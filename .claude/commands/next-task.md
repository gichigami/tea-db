---
description: Pick the next implementation or spec-drafting task per the roadmap and identify the owning specialist
---

Look at `ROADMAP.md`. Find the first unchecked task. Report:

- The task as written in the roadmap
- The owning specialist (from `.claude/agents/`)
- The relevant spec sections to consult
- Any prerequisites that need to be in place first (e.g. "step 5 needs step 1's directory structure", or "spec needed before implementation can begin")

If the next task is **spec drafting** (because no spec exists yet for the area), say so explicitly. Spec drafting blocks implementation in that area; flag this.

Then ask whether to delegate to the specialist now via the Agent tool.
